/*======================================================================
  SUPERSEDED — use SupplierOutstand_Fix_BalanceMismatch.sql instead.

  That script includes the Opening/Closing identity fix PLUS alignment
  with dbo.GetSupplierBalance (PaymentID IN (2,5), DetailAccountID = 26,
  isOpening sign flip, dynamic OpDate).

  Kept so older deploy notes still resolve to a file in this folder.
======================================================================*/
PRINT 'Use SB/Scripts/SupplierOutstand_Fix_BalanceMismatch.sql (this file is superseded).';
GO
